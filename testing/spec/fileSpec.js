describe("Angle Array", function() {
    describe("Generate X Y positions", function() {
        it("should return [1.0, 0.0]", function() {
            expect(getShipLocation(angle)).toBe([1.0, 0.0]);
        });
    });
});